// firebase-config.js — Firebase has been removed.
// This file is kept only to avoid import errors during migration cleanup.
// All auth and data is now handled via Google Sheets + Google Drive.
export const app = null;
export const auth = null;
export const secondaryAuth = null;
